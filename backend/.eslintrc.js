// backend/.eslintrc.js
// The "lint" script in package.json (eslint "{src,apps,libs,test}/**/*.ts")
// has always existed, but no config file did — `npm run lint` failed
// outright with "ESLint couldn't find a configuration file". This is the
// standard Nest CLI-scaffolded config, minus the prettier integration
// (eslint-plugin-prettier / eslint-config-prettier aren't installed here).
module.exports = {
  parser: '@typescript-eslint/parser',
  parserOptions: {
    project: 'tsconfig.json',
    tsconfigRootDir: __dirname,
    sourceType: 'module',
  },
  plugins: ['@typescript-eslint'],
  extends: ['eslint:recommended', 'plugin:@typescript-eslint/recommended'],
  root: true,
  env: {
    node: true,
    jest: true,
  },
  ignorePatterns: ['.eslintrc.js', 'dist', 'node_modules'],
  rules: {
    '@typescript-eslint/interface-name-prefix': 'off',
    '@typescript-eslint/explicit-function-return-type': 'off',
    '@typescript-eslint/explicit-module-boundary-types': 'off',
    '@typescript-eslint/no-explicit-any': 'off',
    '@typescript-eslint/no-unused-vars': ['warn', { argsIgnorePattern: '^_' }],
  },
};
