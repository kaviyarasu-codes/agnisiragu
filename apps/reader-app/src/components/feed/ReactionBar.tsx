// src/components/feed/ReactionBar.tsx
// UNUSED — not wired into the app. SwipeFeed.tsx and FeedCard.tsx were
// reverted to keep using the original ActionBar.tsx (6-icon bar with
// like/dislike/comment counts + WhatsApp/more/share) instead of this
// simplified 4-icon variant. Safe to delete this file — I couldn't remove
// it directly this session (no shell access), so it's left as this empty
// stub; delete it whenever convenient from File Explorer.
export {};
